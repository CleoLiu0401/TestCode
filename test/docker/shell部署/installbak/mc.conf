#基础配置
##本机IP
MC_HOST_IP=1.1.1.1
##镜像仓库IP
MC_REMOTE_IP=1.1.1.1
##安装目录，会自动创建mc_stack目录
MC_INSTALL_PATH=
##资源包存放路径
MC_RESOURCE_PATH=

# kong-db配置
## 数据库名
MC_POSTGRES_DB=kong
## 用户名
MC_POSTGRES_USER=kong
## 密码
MC_POSTGRES_PASSWORD=kong
## 端口
MC_KONG_DB_PORT=5432

# kong-migra配置
## 数据库名
MC_KONG_DATABASE=postgres
## host
MC_KONG_PG_HOST=kong-database
## 密码
MC_KONG_PG_PASSWORD=kong
MC_KONG_CASSANDRA_CONTACT_POINTS=kong-database

# kong配置，外部网关
## 端口
MC_KONG_PORT01=21315
MC_KONG_PORT02=433
MC_KONG_PORT03=8001
MC_KONG_PORT04=8444

# kong-inside-migra配置
## pg数据库名
MC_KONG_PG_DATABASE=kong_inside
## pg数据库密码
# MC_KONG_PG_PASSWORD=kong

# kong-inside配置，内部网关
## 端口
MC_KONG_INSIDE_PORT01=22315
MC_KONG_INSIDE_PORT02=8002

# MYSQL数据库配置
## 数据库地址
MC_MYSQL_HOST=mysql
## 数据库用户名
MC_MYSQL_USER=root
## 数据库密码
MC_MYSQL_PASSWORD=MC-stack@whdata123
## 数据库端口
MC_MYSQL_PORT=3306

# ES配置
##JAVA内存限制
MC_ES_JAVA_OPTS=-Xms1g -Xmx1g
## 端口
MC_ES_PORT01=9200
MC_ES_PORT02=9300
##ES集群节点，多节点英文逗号分割
MC_ES_HOSTS=1.1.1.1:9200
##ES主节点
MC_ES_MASTER=1.1.1.1:9200
##ES用户
MC_ES_USERNAME=elastic
##ES密码
MC_ES_PASSWORD=elastic

# neo4j配置
## 登录认证
MC_NEO4J_AUTH=neo4j/whdata
MC_NEO4J_USERNAME=neo4j
MC_NEO4J_PASSWORD=whdata
## 端口
MC_NEO4J_PORT01=7474
MC_NEO4J_PORT02=7687

# redis配置
## 端口
MC_REDIS_MODE=single
MC_REDIS_PORT=6379
MC_REDIS_PASSWORD=MC-stack@whdata123

# tracker配置
## 端口
MC_TRACKER_PORT=22122

# storage配置
## 端口
MC_STORAGE_PORT01=23002
MC_STORAGE_PORT02=9101


# nginx配置
## 端口
MC_NGINX_PORT01=21308
MC_NGINX_PORT02="21316-21350:21316-21350"

# php配置
## 端口
MC_PHP_PORT01=8082
MC_PHP_PORT02=9000
MC_PHP_PORT03=9999
